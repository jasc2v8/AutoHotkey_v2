    Signal.dat
    RED     =  R (stop  r/w access to shared file)
    GREEN   = -R (start r/w access to shared file)

    Signal init RED				(stop server access, set by Client when finished)
    Service wait GREEN  	(Client set GREEN to allow server access)
    Service set RED     		(stop client access)
    Service read cmd
	Service RunCMD(cmd)
    Service write Output
    Service set GREEN   (client will set to RED, write Output, then GREEN when next cmd is ready)
	short delay to allow Client to set RED before next Service wait GREEN above?
	Service loop

    Signal init RED			(stop server access)
    Loop Signal RED		(stop server access)	
	Client forms cmd
	Client write cmd			(Server will run RunCMD(cmd))
    Client set GREEN      (allow server access, server will set to RED)
	short delay to allow Service to set RED, RunCMD(cmd), write Output, then set GREEN when finished?	
	Client wait GREEN	(wait for Service response
	Client set RED			(stop server access)
    Client read Output		(Server response Output := RunCMD(cmd))
	Client report Output
    Client loop					(Client does NOT set GREEN for server access until next cmd loop above)